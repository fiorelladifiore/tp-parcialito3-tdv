services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: tp-swagger-ui
    ports:
      - "8081:8080"
    environment:
      - SWAGGER_JSON=/openapi/openapi.yaml
      - BASE_URL=/
    volumes:
      - ./openapi/openapi.yaml:/openapi/openapi.yaml:ro
